interface:
  display_name: "Code Review"
  short_description: "Independently review correctness and simplicity"
  default_prompt: "Use $gmgn-v2:code-review and $ponytail:ponytail to independently reconstruct required behavior, challenge this fixed implementation candidate through real call paths and plausible failures, and repair only bounded findings."
